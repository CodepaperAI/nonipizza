# Noni's Pizza & Wings — Website

Production-ready marketing + online-ordering website for **Noni's Pizza & Wings**,
Woodstock, Ontario. Built with **Next.js (App Router) + TypeScript + Tailwind CSS**,
SEO-optimized for local pizza / wings / shawarma search intent.

> **Working in this repo?** Read [`CLAUDE.md`](./CLAUDE.md) first (project source of truth),
> then [`DESIGN.md`](./DESIGN.md) (design system). Log each session in
> [`CONVERSATION.md`](./CONVERSATION.md).

---

## Quick start

```bash
npm install        # install dependencies
npm run dev        # dev server → http://localhost:3000
npm run build      # production build (type-check + static generation)
npm run start      # serve the production build
npm run lint       # eslint
```

Requires Node 18.17+ (developed on Node 22). The first `dev`/`build` downloads the
Anton + Inter fonts via `next/font` — an internet connection is needed once.

---

## Configuration

Copy `.env.example` to `.env.local` and set:

| Variable | Purpose | Default (placeholder) |
| --- | --- | --- |
| `NEXT_PUBLIC_ORDER_URL` | External ordering URL every **Order Now** button opens | `https://www.nonispizza.ca` |
| `NEXT_PUBLIC_SITE_URL` | Canonical site origin (canonical tags, Open Graph, sitemap) | `https://www.nonispizza.ca` |

```bash
cp .env.example .env.local
```

**All** Order Now / Grab Deal / Subscribe CTAs read `NEXT_PUBLIC_ORDER_URL` through
`src/lib/site.ts` — change it in one place and the whole site updates.

---

## Where to swap real photos

The site ships with **branded SVG placeholders** (warm gradient blocks with the dish
name) so every layout is complete without shipping copyrighted stock. They are rendered
by [`src/components/DishImage.tsx`](./src/components/DishImage.tsx).

Each menu item / deal has an `image` slot path (e.g. `/images/pizza-butter-chicken.jpg`)
in `src/data/menu.ts` and `src/data/deals.ts` that **names where the real photo goes**.

To go live with real photography:

1. Drop optimized JPEG/WebP files into `public/images/` at those slot paths.
2. In `DishImage.tsx`, replace the placeholder markup with `next/image`:
   ```tsx
   import Image from "next/image";
   // ...
   <Image src={src} alt={`${name}`} fill className="object-cover" />
   ```
   (pass the item's `image` in as a `src` prop).
3. Update `public/images/og-default.svg` / add a real `og-default.jpg` for social sharing
   and point `src/lib/seo.ts` at it.

Keep `alt` text descriptive (the dish name is already passed in).

---

## Project structure

```
CLAUDE.md · DESIGN.md · CONVERSATION.md   # docs — read CLAUDE.md first
src/
  app/            # routes (App Router) + sitemap.ts + robots.ts
  components/     # NavBar, Hero, MenuSection, DealCard, Footer, ui/ primitives …
  data/           # menu.ts · deals.ts · locations.ts (typed source of truth)
  lib/            # site.ts (config) · seo.ts (metadata) · jsonld.ts (structured data)
public/images/    # placeholder art + real-photo slots
```

Full detail (conventions, menu data model, SEO strategy) lives in `CLAUDE.md`.

---

## Routes

| Route | Purpose |
| --- | --- |
| `/` | Home |
| `/menu` | Full menu + Menu JSON-LD |
| `/deals` | Everyday deals + coupon codes |
| `/pizza-near-me-woodstock` | "pizza near me" local intent |
| `/order-pizza-online-woodstock` | "order pizza online" intent |
| `/pizza-delivery-woodstock` | "pizza delivery" intent |
| `/indian-fusion-pizza-woodstock` | Butter chicken / tandoori — key differentiator |
| `/chicken-wings-woodstock` | Baked wings |
| `/shawarma-woodstock` | Shawarma |
| `/find-us` | Location, hours, map, directions — LocalBusiness + FAQ schema |
| `/reviews` | Testimonials (placeholders — clearly labeled) |
| `/sitemap.xml`, `/robots.txt` | Generated by route handlers |

---

## SEO notes

- Per-page metadata (title/description/canonical/OG/Twitter) via `src/lib/seo.ts`.
- JSON-LD: Restaurant/LocalBusiness (root), Menu (`/menu`), FAQPage + BreadcrumbList
  where relevant — see `src/lib/jsonld.ts`.
- **NAP is identical site-wide**, sourced from `src/data/locations.ts`.
- We deliberately do **not** target competitor brand names — that demand is reframed into
  local intent (see `CLAUDE.md` §7). Review/rating schema is added only when **real**
  reviews exist.

---

## Accessibility

Semantic HTML, skip-to-content link, visible focus rings, descriptive alt text,
WCAG-AA color contrast, keyboard-operable nav/menus, and `prefers-reduced-motion`
support.

---

## License / content

Menu, prices and business details belong to Noni's Pizza & Wings. Placeholder art is
original (no third-party stock). Prices are CAD, exclusive of HST, subject to change.
